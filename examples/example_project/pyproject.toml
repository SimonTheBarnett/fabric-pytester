[project]
name = "fabric-pytester-example-project"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fabric-pytester",
  "pytest>=8",
]

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]
